( function( $ ) {

	// Fire up flexslider
	$( window ).load( function() {

		$( '.flexslider' ).flexslider( {
			animation: "fade",
			slideshowSpeed: 6000,
			directionNav: false,
		} );
	} );

} )( jQuery );
